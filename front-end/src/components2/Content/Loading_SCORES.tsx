
import React, {useState, useEffect, useContext, useCallback, useRef} from 'react';
import {Container} from 'react-bootstrap';

interface Loading_SCORESProps{

}
const Loading_SCORES: React.FunctionComponent<Loading_SCORESProps> =({})=> {

return (
<Container>
    ...Loading
</Container>
);
}
export default Loading_SCORES;
